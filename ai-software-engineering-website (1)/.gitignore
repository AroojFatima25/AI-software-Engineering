node_modules/
dist/

# Local environment files — never commit real keys.
# `.env.example` is tracked on purpose as the documented template.
.env
.env.local
.env.*.local
.env.production
.env.development
!.env.example
